import * as actions from './action'
import * as getters from './getters'
import mutations from './mutation'

const state = {
  addCount: 0,
  subCount: 0
}

export default{
  state,
  actions,
  getters,
  mutations
}
